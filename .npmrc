# Use local workspace checkout of the @schoen scoped package.
# This avoids registry lookup (including self-hosted Gitea) for local lab setups.
# If not present, install tooling from upstream as needed before CI.
